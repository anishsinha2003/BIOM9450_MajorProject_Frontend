import Image from "next/image";

export default function HomePageResearcher() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-zinc-50 font-sans dark:bg-black">
      homePageResearcher has logged in
    </div>
  );
}
